SERVER_IP=192.168.1.168
# Docker 宿主机发布绑定；默认发布 18080/tcp，避开常见 8080 占用。
# 本机临时验证可设为 127.0.0.1:18080。
SYNABOOT_HTTP_BIND=18080
# 应用展示端口，必须是整数。
SYNABOOT_HTTP_PORT=18080
SYNABOOT_DATA_DIR=./data
# 可选：设置强随机值后才启用扫描、生成菜单、创建任务等写接口。
SYNABOOT_ADMIN_TOKEN=
