SERVER_IP=192.168.1.168
# Docker 宿主机发布绑定；默认发布 8080/tcp。
# 本机临时验证可设为 127.0.0.1:18180。
SYNABOOT_HTTP_BIND=8080
# 应用展示端口，必须是整数。
SYNABOOT_HTTP_PORT=8080
SYNABOOT_DATA_DIR=./data
# 可选：设置强随机值后才启用扫描、生成菜单、创建任务等写接口。
SYNABOOT_ADMIN_TOKEN=
